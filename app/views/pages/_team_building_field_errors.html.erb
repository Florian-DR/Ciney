<% messages = inquiry.errors[field].uniq %>
<% if messages.any? %>
  <ul class="team-building-form__field-errors" role="alert">
    <% messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
